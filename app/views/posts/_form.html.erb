<%= simple_form_for @post do |f| %>
  <%= f.input :title %>
  <%= f.input :image %>
  <%= f.input :content, :as => :ckeditor, :input_html => { :ckeditor => {:toolbar => 'Full'} } %>
 <%=  f.button :submit %>
<% end %>
